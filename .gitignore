# Environment files (contains secrets)
.env

# PostgreSQL data (if not using Docker volume)
pgdata/

# Backup files (ignore all, keep folder structure)
backups/**
!backups/.gitkeep

# macOS system files
.DS_Store
