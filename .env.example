# =============================================================================
# PostgreSQL Restore Environment Configuration
# Copy to .env and fill in values. NEVER commit .env with real passwords!
# =============================================================================

# --- Database Credentials ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=          # REQUIRED: Set a secure password
POSTGRES_DB=ncc             # Database name to create/restore into

# --- PostgreSQL Version ---
# Pin version for reproducibility. Available: 15, 16, 17
PG_VERSION=15

# --- Network ---
# Host port mapping (change if 5432 is in use)
PG_PORT=5432

# --- Resource Limits (for local development) ---
# Memory limit (e.g., 256m, 512m, 1g)
PG_MEMORY_LIMIT=512m

# CPU limit (e.g., 0.5, 1.0, 2.0)
PG_CPU_LIMIT=1.0
